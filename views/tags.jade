extends layout

block content
  div.content-header
    h2= tag.sub_title
  div.content-body
    div.article-list
      - for (tag in posts.tags) {
          h4(id=tag) #{tag}(#{posts.tags[tag].length})
        - if (posts.tags.hasOwnProperty(tag)) {
              ul
              - each article in posts.tags[tag]
                li
                  a(href='/p/' + article.path)= article.title
        - }
      - }